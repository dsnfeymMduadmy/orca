================================================================================
  ORCA OS — ПРЕДУПРЕЖДЕНИЯ
  Некритично: собирается и работает, но неправильно / небезопасно /
  неоптимально. Удалять запись ТОЛЬКО после починки И ПОВТОРНОЙ ПРОВЕРКИ.
================================================================================

ФОРМАТ
------
[WARN-NNN] СТАТУС | название
  Где:      файл:строка
  В чём:    описание
  Риск:     что будет, если не чинить
  Чинить:   как
  Проверка: чем убедиться

Статусы: ОТКРЫТА / В РАБОТЕ / ЖДЁТ ПРОВЕРКИ


================================================================================
  ГОНКИ И МНОГОПОТОЧНОСТЬ
================================================================================

Открытых записей нет: WARN-002, WARN-003, WARN-004 закрыты 2026-08-21,
см. раздел «ПОЧИНЕНО И ПРОВЕРЕНО».


================================================================================
  АРИФМЕТИКА И ГРАНИЦЫ
================================================================================

WARN-009 закрыта 2026-08-21, см. раздел «ПОЧИНЕНО И ПРОВЕРЕНО».


================================================================================
  РАСХОЖДЕНИЯ МЕЖДУ РЕАЛИЗАЦИЯМИ ПРОТОКОЛА
================================================================================

[WARN-010] ОТКРЫТА | Парсеры OLP ведут себя по-разному на битом кадре
  Где:      firmware/OrcaKernel/link/orca_link.c        — молча ресинк
            firmware/esp32/components/orca_link/orca_demux.c
                                                        — spill_buffer_as_text()
            tools/fbstream/orca_view.py                 — молча ресинк
  В чём:    при неверной версии или слишком большой длине ESP-демультиплексор
            выплёвывает накопленные байты заголовка в текстовый поток, а
            остальные реализации их просто выбрасывают.
  Риск:     одно и то же событие в разных клиентах выглядит по-разному:
            в консоли ESP появится мусор, в orca_view.py — нет. Отладка
            расхождений съедает время.
  Чинить:   выбрать одно поведение и записать его в orca_link.h как часть
            контракта. Разумнее вариант ESP (не терять байты): «мусор видно» —
            это диагностика, а не проблема.
  Проверка: скормить всем парсерам один и тот же поток с битой версией,
            сравнить и кадры, и текст.
  Правка:   2026-08-20 — из записи убрана четвёртая реализация,
            app/.../link/OlpParser.kt: Android-клиента в репозитории больше
            нет. Парсеров стало три, суть расхождения не изменилась.

[WARN-011] ОТКРЫТА | Структуры payload'ов продублированы в двух языках
  Где:      firmware/OrcaKernel/link/orca_link.h   (истина, packed C)
            tools/fbstream/orca_view.py:58+        (struct.Struct)
  В чём:    размеры совпадают сейчас (Hello 48, LogRecord 20, FbInfo 16,
            FbChunk 8, ShellResponse 8, NetStatus 53, NetConfig 98,
            DeviceError 4), но никакой автоматической сверки нет.
  Риск:     добавление поля в orca_link.h ломает Python молча — данные просто
            разъедутся на смещениях.
  Чинить:   минимум — комментарий-предупреждение в orca_link.h со списком всех
            мест. Лучше — тест, который сверяет struct.calcsize из orca_view.py
            с sizeof из C (например, генерировать эталон при сборке ядра).
  Проверка: изменить поле в orca_link.h — тест должен покраснеть.
  Правка:   2026-08-20 — убрана третья реализация, app/.../link/Payloads.kt:
            Android-клиента в репозитории нет, вместе с ним ушли и готовые
            эталонные константы (Hello.SIZE и т.д.), на которые ссылался
            пункт в todo.txt. Сверять теперь придётся C против Python.


================================================================================
  ПОТЕРЯ ДАННЫХ И ОБРАБОТКА ОШИБОК
================================================================================

WARN-013 и WARN-015 закрыты 2026-08-21, см. раздел «ПОЧИНЕНО И ПРОВЕРЕНО».


================================================================================
  НЕСООТВЕТСТВИЯ КОНФИГУРАЦИИ
================================================================================

WARN-016 и WARN-017 закрыты 2026-08-21, см. раздел «ПОЧИНЕНО И ПРОВЕРЕНО».

[WARN-018] ОТКРЫТА | Часы в файловой системе захардкожены
  Где:      firmware/Core/Src/bsp_diskio.c   get_fattime() -> 2025-01-01
            (в записи было bsp_sdmmc.c — функция переехала, уточнено 2026-08-21)
            firmware/OrcaKernel/api/orca_api_table.c  api_system_unix_time() -> 0
  В чём:    RTC не поднят, поэтому все создаваемые файлы получают одну дату,
            а модули не могут узнать время.
  Риск:     сортировка по дате бесполезна, логи без времени, любая логика
            «раз в сутки» в модуле нереализуема.
  Чинить:   поднять RTC (LSE), синхронизировать через OLP от телефона или ESP.
  Проверка: создать файл, посмотреть дату; api->system->unix_time() должен
            вернуть осмысленное значение.


================================================================================
  ЗАГЛУШКИ (не баг, но знать надо)
================================================================================

[WARN-019] ОТКРЫТА | Существенная часть orca_api_t — заглушки
  Где:      firmware/OrcaKernel/api/orca_api_table.c
  В чём:    группы spi, i2c, ipc и большая часть gui не реализованы.
            Также firmware/Core/Src/bsp_uart.c: bsp_modem_uart_* — заглушки.
  Риск:     docs/module_api.md описывает API целиком, автор модуля напишет код
            под gui/spi и получит нерабочий модуль без внятной причины.
  Чинить:   до реализации — возвращать явный код «не поддержано» и пометить
            в docs/module_api.md, что реализовано, а что нет.
  Проверка: вызвать заглушку из модуля — должен прийти понятный отказ.

[WARN-020] 2026-08-20 ОТПАЛА | Шаблонные файлы Android Studio не адаптированы
  Где было: app/.../ui/theme/Color.kt, Theme.kt (Purple80/PurpleGrey80/Pink80)
            app/.../ExampleUnitTest.kt, ExampleInstrumentedTest.kt
            app/app/src/main/keepRules/rules.keep
  В чём:    оставались дефолты мастера проектов: фиолетовая палитра и тест
            2+2=4. Косметика, риска не несла (заявленная утечка пароля была
            опровергнута — см. запись 2026-08-11 ниже).
  Факт:     каталога app/ в репозитории больше нет, Android-клиент удалён
            целиком. Чинить нечего. Если клиент вернётся — заводить запись
            заново, вместе с проверкой, что шаблонные файлы не приехали
            обратно из мастера проектов.


================================================================================
  ПОЧИНЕНО И ПРОВЕРЕНО (память проекта, не удалять)
================================================================================

[WARN-002] 2026-08-21 ПОЧИНЕНА | orca_input_button_state() читал состояние без защиты
  Где было: firmware/OrcaKernel/gui/orca_input.c, orca_input_button_state()
  В чём:    маску s_buttons пишет задача линка (кадры BUTTON), читает задача
            модуля. Все остальные обращения к общему состоянию в файле уже
            были под taskENTER_CRITICAL — голое чтение здесь оставалось
            исключением, о котором надо помнить при каждой правке структуры
            состояния. Это и есть настоящий риск: не текущая гонка на uint8,
            а то, что следующее поле сделают шире и никто не вспомнит.
  Стало:    чтение копирует маску под критической секцией и уже вне неё
            проверяет бит. Заодно так же закрыт orca_input_dropped(): счётчик
            uint32 читался голым, а инкрементируется он из другой задачи.
  Проверка: cmake --build build -j — чисто. Перечитаны все функции файла:
            обращений к s_queue/s_head/s_tail/s_count/s_buttons/s_last вне
            критической секции больше нет.

[WARN-003] 2026-08-21 ПОЧИНЕНА | Статические буферы в on_shell_cmd без явного инварианта
  Где было: firmware/OrcaKernel/services/orca_link_service.c, on_shell_cmd()
  В чём:    static char out[SHELL_OUT_MAX] и static uint8_t resp[...] работали
            только потому, что обработчик зовётся из одной задачи. Нигде это
            не было записано, то есть второй источник команд (shell из USB,
            второй линк) молча портил бы чужой ответ.
  Стало:    задача линка запоминает себя (s_task = xTaskGetCurrentTaskHandle()
            в orca_link_service_task), а on_shell_cmd открывается
            configASSERT(s_task == NULL || s_task == xTaskGetCurrentTaskHandle())
            с комментарием, что делать при появлении второго вызывающего.
            Инвариант теперь проверяется, а не описывается.
  Проверка: cmake --build build -j — чисто. Вызовы on_shell_cmd перечитаны:
            единственный — из dispatch(), который зовётся только из
            orca_link_service_task.

[WARN-004] 2026-08-21 ПОЧИНЕНА | bsp_sdmmc: общий выровненный буфер без мьютекса
  Где было: firmware/Core/Src/bsp_sdmmc.c   s_align_buf
  В чём:    буфер под невыровненные обмены один на всю систему, а вызывающих
            трое: FatFs (со своим мьютексом), USB MSC и прямые вызовы
            bsp_sdmmc_* из ядра. Мьютекса FatFs хватало только на FatFs.
  Стало:    свой мьютекс s_lock в bsp_sdmmc. Тела вынесены в
            sdmmc_block_read_locked / sdmmc_block_write_locked, публичные
            bsp_sdmmc_block_read/write/sync берут блокировку. До старта
            планировщика (init -> mount -> disk_read) sd_lock() честно
            возвращает false и не блокирует: там мы гарантированно одни, а
            xSemaphoreTake до scheduler'а — это assert внутри FreeRTOS.
  Проверка: cmake --build build -j — чисто. Одновременное чтение из двух
            задач со сверкой контрольных сумм на железе не гонялось.

[WARN-009] 2026-08-21 ПОЧИНЕНА | Конверсия кадра доверяла размерам от вызывающего
  Где было: firmware/OrcaKernel/link/orca_fbstream.c, orca_fbstream_push_frame()
  В чём:    ни одной проверки, что width/height/stride соответствуют реальному
            буферу. Вызывающий был один и честный, но подпись функции публичная
            и лежит в orca_fbstream.h: первый же новый источник кадра (LTDC,
            модуль) мог задать геометрию побольше и читать чужую SDRAM.
  Стало:    в подпись добавлен fb_bytes — размер буфера в байтах. Перед
            конверсией проверяется stride >= width*SRC_BPP и что последний
            читаемый байт (out_h-1)*scale*stride + ((out_w-1)*scale+1)*SRC_BPP
            не выходит за fb_bytes. Считаем именно последний байт, а не
            height*stride: при scale>1 читается не весь буфер.
            Кадр дропается со счётчиком frames_dropped, а причина печатается
            один раз (s_geom_reported): при 30 fps лог иначе смывает всё
            остальное, а сообщение одно и то же.
  Стало-2:  оба вызывающих обновлены: orca_lvgl.c передаёт GUI_BYTES,
            orca_fbtest.c — новый FBT_BYTES (FBT_STRIDE*FBT_H). Размер идёт
            из той же макроконстанты, из которой объявлен сам буфер, поэтому
            разъехаться они не могут.
  Проверка: cmake --build build -j — чисто. Завышенный height на железе не
            подсовывался: логика границы проверена чтением, оба вызывающих
            статические.

[WARN-013] 2026-08-21 ПОЧИНЕНА | orca_arena_free() молча игнорировал чужой указатель
  Где было: firmware/OrcaKernel/services/orca_arena.c, orca_arena_free()
  В чём:    указатель не из арены и повторный free выходили без следа. Утечка
            выглядела как «памяти почему-то нет», а искать её приходилось
            с нуля.
  Стало:    в критической секции запоминаются два факта (found, double_free),
            а печатается предупреждение уже ПОСЛЕ arena_unlock(). Это не
            стилистика: printf уходит в UART под мьютексом, вызов из-под
            taskENTER_CRITICAL уронил бы систему по configASSERT.
  Проверка: cmake --build build -j — чисто. free с левым адресом на железе
            не вызывался.

[WARN-015] 2026-08-21 ПОЧИНЕНА | _sbrk() без верхней границы кучи
  Где было: firmware/Core/Src/retarget.c, _sbrk()
  В чём:    куча newlib росла от _end вверх без ограничения. Падало не на
            malloc, а позже — на записи в мусорный адрес.
  Стало:    в линкере PROVIDE(_heap_limit = ORIGIN(RAM_D1) + LENGTH(RAM_D1)),
            в _sbrk сравнение с ним и errno = ENOMEM при исчерпании, плюс
            отсев incr < 0 (EINVAL). Сравнение сделано в остатке
            (incr > _heap_limit - heap_end), а не в сумме: heap_end + incr
            переполняется и проверка «больше лимита» тогда не срабатывает.
            Границей взят конец RAM_D1, а не __StackLimit: стек MSP живёт в
            DTCM, кучей его не достать, а вот за конец региона malloc уезжал.
  Проверка: arm-none-eabi-nm — _end = 0x2402ccd8, _heap_limit = 0x24080000,
            запас около 333 КБ. Сборка чистая.

[WARN-016] 2026-08-21 ПОЧИНЕНА | MPU-регион меньше объявленной области RAM_D2
  Где было: firmware/Core/Src/board.c (регион 4), RAM_D2 288K @0x30000000
  В чём:    MPU закрывал 256 КБ, линкер объявлял 288 КБ; размер региона обязан
            быть степенью двойки, поэтому 288 КБ одним регионом не закрыть.
            Действующего бага не было (проверено 2026-08-11, HYP-007), запись
            жила как латентная: новый DMA-буфер за 255 КБ уехал бы в
            кешируемую зону.
  Стало:    регион 5 на 32 КБ по 0x30040000 с теми же атрибутами
            (non-cacheable, non-bufferable, no-exec) — хвост RAM_D2 закрыт
            целиком. Вариант «урезать RAM_D2 в линкере» отброшен: он отдаёт
            32 КБ памяти ради той же цели.
  Проверка: cmake --build build -j — чисто; в orca_kernel.map единственная
            секция RAM_D2 — .dma_buffer 0x30000000, 0x400 байт, внутри
            региона 4. Обращение к 0x3004xxxx на железе не проверялось.

[WARN-017] 2026-08-21 ПОЧИНЕНА | Комментарий в загрузчике обещал символы __aeabi_*
  Где было: firmware/OrcaKernel/loader/orca_loader.c, таблица символов ядра
  В чём:    комментарий обещал поддержку uint64/float, а в таблице стоял один
            atoi. Модуль с 64-битным делением получал
            ORCA_LOAD_ERR_UNDEFINED_SYMBOL, причём разработчик по комментарию
            был уверен в обратном.
  Стало:    22 записи __aeabi_* (uldivmod/ldivmod, uidiv/idiv/uidivmod/idivmod,
            lmul, llsl/llsr/lasr, ddiv/dmul/dadd/dsub, d2lz/d2ulz/l2d/ul2d,
            f2lz/f2ulz/l2f/ul2f) плюс externs с минимальными прототипами:
            настоящие сигнатуры нестандартные (uldivmod возвращает 128 бит в
            r0-r3), а нужен только адрес. Само упоминание в таблице заставляет
            линкер втянуть хелперы из libgcc — ядро само 64-битным делением
            не пользуется и без этого их бы не содержало.
  Проверка: arm-none-eabi-nm build/orca_kernel.elf — все хелперы в образе.
            Отдельно собран пробный модуль флагами SDK, использующий uint64
            и double: ему потребовались __aeabi_d2lz, __aeabi_d2ulz,
            __aeabi_ldivmod, __aeabi_ul2d, __aeabi_uldivmod — все пять в
            таблице. Запуск на железе командой run не выполнялся.
            Закрывает и HYP-010.

[WARN-006] 2026-08-20 ПОЧИНЕНА | uart_handle_to_port() принимал несуществующие порты
  Где было: firmware/OrcaKernel/api/orca_api_table.c, uart_handle_to_port()
  В чём:    валидными считались теги UART_HANDLE_BASE+0..7, тогда как в
            bsp_uart.c реализован ровно один модемный порт (port_id 0).
            Модуль, передавший хендл BASE+3, проходил валидацию и уезжал в BSP
            с несуществующим индексом — то есть ошибка всплывала на уровне,
            где port_id уже никто не проверяет.
  Стало:    константа UART_PORT_COUNT = 1 рядом с UART_HANDLE_BASE, проверка
            `tag >= BASE + UART_PORT_COUNT` вместо `tag > BASE + 7`. Заодно
            api_uart_open() отбивает port_id >= UART_PORT_COUNT: раньше
            невалидный порт защищала только заглушка bsp_modem_uart_open,
            которая всегда возвращает false, — при появлении драйвера дыра
            открылась бы снова.
  Проверка: cmake --build build -j — сборка чистая, новых предупреждений нет.
            Проверка модулем на железе не выполнялась (нет UART-драйвера,
            open() пока честно отвечает отказом — WARN-019).

[WARN-007] 2026-08-20 ПОЧИНЕНА | Нет нижней границы стека задачи модуля
  Где было: firmware/OrcaKernel/services/orca_appmgr.c:81
  В чём:    stack_size брался из module.json как есть. "stack": 64 давало
            16 слов — не хватает даже на пролог задачи, и модуль ронял систему
            переполнением стека, где настоящая причина (опечатка в манифесте)
            никак не видна.
  Стало:    клампим снизу к configMINIMAL_STACK_SIZE (128 слов) и пишем в лог
            warning с тегом "appmgr", называя и запрошенное значение, и
            минимум. Молчаливая правка чужого числа хуже самой опечатки:
            автор модуля должен узнать, что его манифест исправили.
  Проверка: cmake --build build -j — чисто. Логика клампа проверена чтением:
            путь до xTaskCreate один, стек считается ровно в одном месте.
            Запуск модуля с stack 64 на железе — за следующим сканом.

[WARN-008] 2026-08-20 ПОЧИНЕНА | Слабый порог дропа кадра в fbstream
  Где было: firmware/OrcaKernel/link/orca_fbstream.c, перед отправкой FB_INFO
  В чём:    писалось `writable() < ORCA_LINK_HEADER_SIZE`, то есть спрашивали
            место под 8 байт заголовка. Ответ «место есть» получался почти
            всегда, нехватка обнаруживалась уже посреди записи, и кадр рвался
            на середине — ровно то, что файл обещает не делать.
  Стало:    порог — полный размер ближайшего кадра: ORCA_LINK_HEADER_SIZE +
            sizeof(orca_link_fb_info_t) + ORCA_LINK_CRC_SIZE. Считается через
            sizeof, а не числом: добавление поля в FB_INFO не разъедется с
            порогом.
  Проверка: cmake --build build -j — чисто.
            Трансляция 1:1 на 921600 на железе не гонялась.
  Осталось: порог закрывает только FB_INFO. Чанки по-прежнему могут не влезть
            в середине кадра — это WARN-009 и вопрос политики дропа (HYP-004).

[WARN-014] 2026-08-20 ПОЧИНЕНА | Бинарное ИЛИ по кодам esp_err_t
  Где было: firmware/esp32/main/orca_cfg.c, orca_cfg_save()
  В чём:    err |= nvs_set_*(...) по девяти ключам. esp_err_t — код, а не
            набор флагов: ИЛИ собирало число, которого нет в таблице, и
            esp_err_to_name() в логе ниже печатал бы про него "UNKNOWN ERROR",
            то есть причина сбоя сохранения настроек терялась.
  Стало:    статический хелпер keep_first_err(&err, ...) — запоминает ПЕРВУЮ
            ошибку. Писать продолжаем до конца: ключи независимы, и половина
            сохранённого конфига полезнее обрыва на первом неудачном ключе.
  Проверка: только чтением. idf.py build НЕ запускался: ESP-IDF нет в PATH
            (то же препятствие, что у ERR-001).

[WARN-001] 2026-08-20 ЗАКРЫТА | Счётчики клиентов на ESP
  Где было: esp32/main/orca_tcp.c:27, esp32/main/orca_web.c:37 — volatile
            uint32_t с ++/-- из двух задач.
  Факт:     к моменту проверки правка уже в коде: orca_tcp.c держит счётчик
            обычным uint32_t, а меняет и читает его через __atomic_add_fetch /
            __atomic_load_n (строки 25, 34, 192). Read-modify-write больше не
            рвётся.
            Вторая половина записи отпала вместе с файлом: orca_web.c в
            репозитории нет, веб-морда и её s_ws_clients удалены.
  Проверка: чтением. Скрипт на 50 подключений не гонялся: GET /api/status
            больше не существует, проверять нечем — статус клиентов теперь
            смотрят через orca_ctl.

[WARN-005] 2026-08-20 ЗАКРЫТА | api_storage_fsinfo(): переполнение на картах > 4 ГБ
  Где было: firmware/OrcaKernel/api/orca_api_table.c, api_storage_fsinfo()
  Факт:     правка уже в коде (строки 309-310): оба произведения приводятся к
            uint64_t до умножения, поля out->total_bytes / out->free_bytes
            64-битные. Рядом лежит комментарий с причиной. Запись просто не
            закрыли в прошлый раз.
  Проверка: чтением. Карта >4 ГБ к сверке `free` с реальным объёмом не
            подключалась.

[WARN-012] 2026-08-11 ПОЧИНЕНА | Android: отправка молча теряла кадры
  Было:     outbox.trySend(...) — результат игнорировался; writeLoop делал
            continue, если связь не ONLINE; OrcaRepository звал client?.send*,
            и при незапущенном сервисе вызов уходил в null без следа.
            Три независимых пути потери на одну кнопку в UI.
  Стало:    OrcaClient.kt — канал теперь несёт тип кадра (private class
            Outgoing), появилось событие OrcaEvent.SendFailed(type, reason).
            Отчёт шлётся из трёх точек: переполнение очереди (trySend.isFailure),
            offline и исключение transport.write. OrcaRepository.withClient()
            закрывает четвёртый путь — отсутствие клиента.
            TOUCH намеренно молчит: следующее движение пальца перезапишет
            позицию, а сообщение на каждый пропущенный MOVE забьёт консоль.
            Ёмкость очереди задана явно: OUTBOX_CAPACITY = 128 вместо
            Channel.BUFFERED (64) — хватает на всплеск тача ~120 событий/с.
  Проверка: OrcaClientTest.sendReportsFailureWhenDeviceIsUnreachable —
            фейковый транспорт, который не открывается; шлём TOUCH и
            NET_CONFIG, ждём ровно один SendFailed и именно про NET_CONFIG.
            Тест проверен на честность: с возвращённым старым writeLoop он
            падает. 27/27 юнит-тестов зелёные, app-debug.apk 19 257 613 байт.
  Осталось: буферизации «до подключения» нет и не планируется — команда,
            выполненная через минуту после нажатия, хуже честной ошибки.

[WARN-020] 2026-08-11 ЧАСТИЧНО | правила бэкапа — формулировка риска была ошибочной
  Было в записи: «правила бэкапа пустые, то есть в облако уедет весь DataStore,
            включая пароль Wi-Fi, который приложение отправляет в NET_CONFIG».
  Факт:     пароля в DataStore нет. SettingsRepository хранит ровно восемь
            ключей: host, port, auto_connect, stream_enabled, pixfmt, scale,
            max_fps, glyph_* — и ни одного поля пароля. В DeviceScreen.kt
            пароль живёт в remember { mutableStateOf("") }, то есть умирает
            вместе с композицией и на диск не попадает вообще. Утечки не было.
  Сделано:  backup_rules.xml и data_extraction_rules.xml всё же переписаны —
            но БЕЛЫМ СПИСКОМ (<include> на datastore/orca_settings.
            preferences_pb), а не <exclude>, как предлагала запись.
            Исключать файл настроек было бы вредно: он не содержит секретов,
            зато его восстановление избавляет от повторного ввода host/port.
            Белый список даёт то, ради чего запись заводилась: файл с секретом,
            если он появится завтра, не уедет в облако сам собой.
  Проверка: сборка проходит, файлы валидируются AAPT (ошибка в схеме этих
            XML ломает merge ресурсов).
  Осталось: косметика — палитра и шаблонные тесты, запись выше.

[HYP-012] 2026-08-11 ПОЧИНЕНА | Android: fps застывал при тишине в линии
  Где было: app/.../link/OrcaClient.kt, readLoop — `if (n == 0) continue`
  В чём:    TcpTransport.read() отдаёт 0 по SocketTimeoutException (soTimeout
            1 с), то есть при живом сокете без кадров цикл просыпался раз в
            секунду и тут же уходил на следующую итерацию, минуя
            publishStats(). LinkStats.fps оставался равен последнему
            посчитанному значению. Выключенная трансляция в UI выглядела
            идущей — худший вид ошибки в диагностическом приложении.
  Стало:    publishStats() вызывается на каждой итерации, в том числе пустой.
            Лишней работы нет: внутри стоит `if (elapsed < 500) return`,
            то есть при таймауте раз в секунду тело выполняется раз в секунду.
  Проверка: OrcaClientTest.statsAreRefreshedWhileLineIsSilent — фейковый
            транспорт отдаёт один кадр PONG и дальше возвращает 0, как сокет
            по таймауту; тест ждёт framesOk == 1 при fps == 0. С возвращённым
            `if (n == 0) continue` тест падает — проверено намеренной поломкой.

[HYP-016] 2026-08-11 ПОЧИНЕНА | orca_view.py: деление на ноль по высоте кадра
  Где было: tools/fbstream/orca_view.py, масштабирование в GUI-режиме
  В чём:    в условии подбора целого масштаба `w` был защищён проверкой,
            а `h` — нет. Кадр с height == 0 (битый FB_INFO) ронял GUI
            через ZeroDivisionError. Валидации размеров выше по коду не было.
  Стало:    отсев перенесён в начало show(): `if w <= 0 or h <= 0: return`.
            Это шире, чем `and h` в одном условии, и закрывает заодно ветку
            с Pillow, где деления нет, но Image.frombytes на нулевом размере
            тоже падает.
  Проверка: python3 -m py_compile проходит. Запуска GUI не было: на этой
            машине нет ни DISPLAY, ни Xvfb, поэтому tkinter-код проверен
            только синтаксически — см. todo.txt.
