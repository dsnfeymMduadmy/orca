================================================================================
  ORCA OS — TODO
  Конкретика по коду. Майлстоуны живут в docs/TASKS.md, здесь — что именно
  открыть и что именно написать.
================================================================================

КАК ПОЛЬЗОВАТЬСЯ
----------------
Приоритеты:
  P0 — без этого часть проекта не собирается или не работает вообще.
  P1 — работает, но неправильно; чинить в первую очередь после P0.
  P2 — нужное развитие функциональности.
  P3 — удобство, чистота, «когда-нибудь».

Сделанный пункт НЕ удаляется, а переезжает в раздел «СДЕЛАНО» внизу с датой.
Так видно историю и не приходится вспоминать, чинили ли уже.

Каждый пункт ссылается на запись в errors.txt / warnings.txt /
i-think-it-can-be-error.txt, если она есть. Ссылка — это не украшение: закрывая
пункт, надо закрыть и связанную запись (с перепроверкой).


================================================================================
  P0 — БЛОКИРУЮЩЕЕ
================================================================================

[ ] Собрать ESP32 на железе                                   -> ERR-001 (закрыта)
    Формулировка изменилась 2026-08-21: веб-морды больше нет, ERR-001 отпала
    вместе с orca_web.c и app.js. Но блокирующее осталось то же самое: код
    ESP32 (firmware/esp32) не собирался ни разу, ESP-IDF не установлен
    (проверено 2026-08-21: command -v idf.py и $IDF_PATH пусты). Значит все
    правки в esp32/ — включая WARN-001, WARN-014 и HYP-002 — проверены только
    чтением.
    source $IDF_PATH/export.sh && cd firmware/esp32 && idf.py build
    Готово, когда: сборка проходит, мост прошит, `orca_ctl` отвечает, а
    трансляция кадров доходит до клиента через TCP.


================================================================================
  P1 — НЕПРАВИЛЬНО РАБОТАЕТ
================================================================================

Открытых пунктов нет (2026-08-21). Все тринадцать разошлись так:
починены — ERR-002, ERR-003, HYP-005, WARN-001, WARN-005, WARN-006, WARN-007,
WARN-008, WARN-014, HYP-016; отпали вместе с удалёнными подсистемами —
HYP-001 (веб-морда ESP), WARN-012 и WARN-020 (Android-клиент).
Подробности — в «СДЕЛАНО» ниже и в errors.txt / warnings.txt.


================================================================================
  P2 — РАЗВИТИЕ ФУНКЦИОНАЛЬНОСТИ
================================================================================

ЯДРО: то, чего физически нет
----------------------------
[ ] Дисплей. Milestone 2 не начат целиком.
    LTDC для LCD1 (40-pin RGB) с двойной буферизацией в SDRAM — место под
    это уже зарезервировано (FB_POOL 4 МБ в orca_memmap.h, кадр 480x320x2 =
    300 КБ, влезает четыре).
    SPI-драйвер для LCD2 (20-pin) + I2C тачскрин.
    Пока дисплея нет, единственный способ увидеть картинку — трансляция
    fbstream на телефон/PC, и рисует туда только тестовая задача fbtest.

[ ] GUI-движок: примитивы (rect/line/text), шрифты, виджеты (список, кнопка).
    Группа gui в orca_api_t сейчас почти вся заглушка -> WARN-019.

[ ] Системный launcher — главный экран со списком приложений с SD.

[ ] Реализовать группы spi / i2c / ipc в orca_api_table.c    -> WARN-019
    До реализации — возвращать явный код «не поддержано», а не тихий успех,
    и пометить в docs/module_api.md, что готово, а что нет. Иначе автор
    модуля пишет код под несуществующее API.

[ ] bsp_modem_uart_* в firmware/Core/Src/bsp_uart.c — заглушки.

[ ] RTC на LSE                                               -> WARN-018
    Сейчас get_fattime() отдаёт захардкоженное 2025-01-01, а
    api_system_unix_time() — ноль. Значит: все файлы на SD с одной датой,
    логи без времени, любая логика «раз в сутки» в модуле нереализуема.
    Синхронизацию времени добавить в OLP (кадром от телефона или от ESP).

[ ] I2C EEPROM + key-value для настроек (Milestone 1, не начато).

[ ] orca_vfs — единая точка монтирования SD: / QSPI: / EEPROM:
    Сейчас fs/orca_fileio.c ходит в FatFs напрямую.

ЯДРО: устойчивость (Milestone 3, хвост)
---------------------------------------
[ ] Изоляция ошибок модуля: HardFault/MemManage внутри модуля должен убивать
    задачу модуля, а не ронять систему. Сейчас stm32h7xx_it.c уводит всё в
    orca_panic — то есть кривой модуль убивает часы.

[ ] Watchdog на задачу: таймаут для зависшего модуля.

[ ] Graceful stop. В docs/TASKS.md записано, что orca_appmgr_stop сразу
    зовёт vTaskDelete. По коду claim_teardown() выглядит аккуратнее, чем
    описано, — надо перечитать и либо обновить TASKS.md, либо дописать
    ожидание самостоятельного выхода задачи через event group.
    Пока модуль не успевает освободить GPIO/UART/SPI.

[x] Пополнить таблицу символов загрузчика                    -> WARN-017, HYP-010
    Сделано 2026-08-21, подробности в «СДЕЛАНО».

БЕЗОПАСНОСТЬ (Milestone 5, не начато)
-------------------------------------
[ ] Контрольная сумма/подпись .orca — сейчас загружается что угодно с SD.
[ ] permissions из module.json -> реальные проверки доступа к группам API.
    Поле в манифесте есть, проверок нет.
[ ] MPU-регионы под модуль: код и данные модуля без доступа к памяти ядра
    и соседних модулей.
[ ] Лимиты памяти и CPOU-квоты на модуль.

ESP32
-----
[ ] Политика дропа в orca_hub на границах кадров              -> HYP-004
    HYP-002 (накопление задач wifi_retry) закрыт 2026-08-21.

TOOLS
-----
[ ] tools/elf2orca — упаковка и валидация манифеста, контрольные суммы
    (Milestone 4). Каталог tools/elf2orca появился, содержимое не проверялось.
[ ] Тест на совпадение размеров payload'ов между C и Python  -> WARN-011
    Эталонные константы жили в Kotlin (Hello.SIZE и т.д.) — вместе с app/
    они исчезли. Сверять теперь надо orca_link.h против orca_view.py, и
    писать сверку с нуля.


================================================================================
  P3 — ЧИСТОТА И УДОБСТВО
================================================================================

[ ] Инициализировать git.                                    -> HYP-015 (отпала)
    Репозиторий не создан вообще (перепроверено 2026-08-21). В .gitignore
    обязаны попасть:
      firmware/build/            (из них почти всё — _deps)
      firmware/esp32/build/  firmware/esp32/sdkconfig
      tools/fbstream/__pycache__/
      tools/module_sdk/**/build/
      .aider.chat.history.md  .aider.input.history
    Строки про app/ больше не нужны: Android-клиента в дереве нет.
    Без этого первый же коммит утащит сотни мегабайт скачанных зависимостей.

[ ] Удалить мусор предыдущей попытки: .aider.chat.history.md,
    .aider.input.history, tools/fbstream/__pycache__/.

[ ] Обновить firmware/Middlewares/README.md. Он описывает ручную раскладку
    вендорного кода, которой больше нет: всё тянет FetchContent, папка пуста.

[ ] Синхронизировать docs/TASKS.md с реальностью. Пункт про THM_CALL уже
    отмечен сделанным (проверено 2026-08-21), а вот orca_api_table.c всё ещё
    описан как «только объявления» при 745 строках реализации. Заодно
    вычистить из docs упоминания веб-морды и Android-клиента.

[ ] Договориться о поведении парсеров OLP на битом кадре     -> WARN-010
    Реализаций стало три (ядро, ESP, orca_view.py): две молча ресинкаются,
    ESP выплёвывает байты как текст. Выбрать одно и записать в orca_link.h
    как часть контракта. Вариант ESP разумнее: мусор в консоли — это
    диагностика.

[ ] Измерить стоимость обхода free-list под critical         -> HYP-006
[ ] Проверить границы цикла memtest SDRAM                    -> HYP-008
[ ] Решить вопрос tearing между fbtest и fbstream            -> HYP-009
[ ] Флаг --reset-scheme в orca_view.py                       -> HYP-017

Закрыто 2026-08-21 (было в этом разделе): WARN-016, WARN-004, WARN-013,
WARN-015, WARN-003, WARN-002, WARN-009. Отпали вместе с app/: WARN-020
(шаблоны Android Studio), пункт про strings.xml и локализацию.

BACKLOG (из docs/TASKS.md, сюда просто чтобы не потерялось)
-----------------------------------------------------------
[ ] OTA обновление ядра через QSPI: двойной образ + fallback.
[ ] IPC между модулями (GUI-приложение просит данные у модуля esp_link).
[ ] Второй дисплей как отдельный режим: компактные часы против основного UI.
[ ] Меню настроек в GUI: Wi-Fi креды, яркость, время.
[ ] Менеджер модулей в GUI: список, установка с SD, удаление, логи модуля.
[ ] Экран «О системе»: версия ядра, свободная память, версия раскладки SD.


================================================================================
  СДЕЛАНО (не удалять — это память проекта)
================================================================================

[x] 2026-08-21  Девять дефектов ядра починены и пересобраны   -> WARN-002..017
    WARN-002 — чтение маски кнопок и счётчика dropped ушло под критическую
    секцию (orca_input.c): важна не гонка на uint8 сегодня, а то, что поле
    рано или поздно сделают шире.
    WARN-003 — инвариант «у on_shell_cmd один вызывающий» теперь
    проверяется configASSERT по s_task, а не описывается словами.
    WARN-004 — у s_align_buf в bsp_sdmmc свой мьютекс: FatFs, USB MSC и
    прямые вызовы ядра больше не делят буфер на честном слове.
    WARN-009 — orca_fbstream_push_frame() принимает fb_bytes и считает
    последний читаемый байт, поэтому геометрия от вызывающего больше не
    даёт чтения чужой SDRAM.
    WARN-013 — orca_arena_free() сообщает про чужой указатель и двойной
    free, печатая ПОСЛЕ arena_unlock() (printf берёт мьютекс UART).
    WARN-015 — _sbrk() ограничен _heap_limit из линкера, сравнение в
    остатке, чтобы не переполнить сумму; запас около 333 КБ.
    WARN-016 — хвост RAM_D2 накрыт вторым MPU-регионом, 32 КБ @0x30040000.
    WARN-017 и HYP-010 — в таблицу символов загрузчика добавлены 22 хелпера
    __aeabi_*, в SDK появилась цель make check-symbols.
    Проверка: cmake --build build -j — чисто. FLASH 637 328 Б (30.39%),
    RAM_D1 232 672 Б, единственная секция RAM_D2 — .dma_buffer
    0x30000000 размером 0x400. На железе не проверялось ничего из списка:
    границы, MPU-доступ к 0x3004xxxx, одновременное чтение SD.
    Подробности по каждой — warnings.txt, «ПОЧИНЕНО И ПРОВЕРЕНО».

[x] 2026-08-21  Задача wifi_retry больше не размножается     -> HYP-002
    Каждое WIFI_EVENT_STA_DISCONNECTED после пяти быстрых попыток рождало
    ещё одну задачу по 2 КБ стека, и все они через 30 с дёргали
    esp_wifi_connect(). Создание закрыто флагом s_retry_pending, флаг
    откатывается при неудаче xTaskCreate — иначе единственный сбой создания
    навсегда запретил бы медленный ретрай.
    НЕ проверено сборкой: ESP-IDF нет в PATH, см. единственный P0.

[x] 2026-08-21  SDK модулей перестал противоречить коду  -> ERR-002, ERR-003
    Из docs/architecture.md §3 убрано «модули собираются с -mlong-calls,
    THM_CALL пока не поддержан» — последнее место, где жило расхождение
    ERR-002: в Makefile его починили раньше, а в архитектурный документ
    оно переехало и осталось. ERR-003 закрыт существованием
    tools/module_sdk/examples/esp_link — ссылки из README и docs больше не
    ведут в никуда.
    Проверка: make в каталоге примера — esp_link.orca 1700 Б без единого
    -mlong-calls в командной строке; make check-symbols печатает пустой
    список неразрешённых символов. Артефакты build/ удалены после проверки.
    Запуск .orca на железе командой run не выполнялся.

[x] 2026-08-21  Шесть гипотез закрыты без починки           -> HYP-005 и др.
    HYP-005 опровергнута: терминатор strtab_buf в загрузчике уже стоял,
    запись просто не закрыли в прошлый раз. HYP-001, HYP-003 отпали вместе
    с orca_web.c, HYP-013, HYP-014, HYP-015 — вместе с каталогом app/.
    Подробности — i-think-it-can-be-error.txt, «ПРОВЕРЕНО И ЗАКРЫТО».

[x] 2026-08-11  Написан esp32/main/web/app.js                 -> ERR-001
    804 строки, 27 408 байт. Все восемь пунктов спецификации: WebSocket на
    /ws в бинарном режиме, разбор OLP с CRC16-CCITT-FALSE, сборка кадра
    FB_INFO + N*FB_CHUNK + FB_END, конверсия RGB565/RGB888/GRAY8 в ImageData,
    отправка FB_REQ/TOUCH/BUTTON/SHELL/PING/HELLO, панели устройства и линка,
    консоль с автопрокруткой, четыре REST-вызова, вкладки по data-tab.
    Сверено статически: node --check, CRC на эталоне "123456789" -> 0x29B1,
    все id из $() есть в index.html, смещения полей совпадают с packed-
    структурами orca_link.h, маршруты совпадают с orca_web.c:400-403.
    НЕ проверено на железе: idf.py build и браузер — см. пункт в P0.

[x] 2026-08-11  Android собирается, тесты зелёные            -> HYP-011 закрыта
    ./gradlew :app:assembleDebug :app:testDebugUnitTest -> BUILD SUCCESSFUL
    за 12 с на JDK 21.0.11. Версии из libs.versions.toml (AGP 9.2.1,
    Gradle 9.4.1, Kotlin 2.2.10, compileSdk 37, Compose BOM 2026.02.01)
    резолвятся, понижать ничего не нужно. app-debug.apk 19 240 977 байт.
    Юнит-тесты 24/24: OlpTest 16, FrameAssemblerTest 7, ExampleUnitTest 1.
    Теперь про app/ можно утверждать хоть что-то: оно как минимум живое.

[x] 2026-08-11  DMA-секции в RAM_D2 лежат правильно          -> HYP-007 закрыта
    В orca_kernel.map:9643-9649 единственная секция RAM_D2 — .dma_buffer,
    0x30000000..0x30000400, 1 КБ (bsp_sdmmc.c 0x200 + bsp_uart.c 0x200).
    Запас до границы MPU 0x30040000 — 255 КБ. Повреждения данных на SD,
    которого боялись, нет. Остаток WARN-016 переехал в P3 как латентный.


================================================================================
  ИСТОРИЯ СКАНОВ
================================================================================

Скан #1 — первый полный.
  Охват:    весь репозиторий, включая исходники firmware/ (Core + OrcaKernel),
            esp32/ (main + components), app/ (все .kt, gradle, res, тесты),
            tools/, sdcard_root/, docs/.
            Не читались: firmware/build/_deps/** (скачанные HAL/CMSIS/
            FreeRTOS/FatFs — вендорный код, не наш), бинарные артефакты,
            app/.gradle и app/build (кеши).
  Проверки: firmware — `cmake --build build -j` -> ninja: no work to do,
            артефакты на месте (orca_kernel.elf 2.79 МБ, .bin 92 972 байта).
            esp32 — сборка НЕ запускалась (не установлен ESP-IDF), но
            отсутствие web/app.js подтверждено через ls и grep по четырём
            местам использования.
            app — сборка gradle НЕ запускалась ни разу.
  Итог:     3 записи в errors.txt, 20 в warnings.txt,
            17 в i-think-it-can-be-error.txt.
  Создано:  promt.txt, info.txt, errors.txt, warnings.txt,
            i-think-it-can-be-error.txt, todo.txt.

Скан #2 — 2026-08-11, точечный: только три пункта P0.
  Охват:    не полный скан. Проверялись ровно блокирующие пункты:
            esp32/main/web/app.js (появился после скана #1, сверен с
            index.html, orca_link.h, orca_web.c), сборка app/ через gradle,
            размещение секций в firmware/build/orca_kernel.map.
  Проверки: app — ./gradlew :app:assembleDebug :app:testDebugUnitTest ->
            BUILD SUCCESSFUL, 24/24 теста, apk 19 240 977 Б.
            app.js — node --check OK, CRC на эталонном векторе 0x29B1,
            сверка id с разметкой и смещений с packed-структурами.
            esp32 — idf.py build ПО-ПРЕЖНЕМУ не запускался: ESP-IDF нет
            в PATH. Это единственное, что осталось от ERR-001.
            firmware — пересборка не запускалась, читалась только карта.
  Итог:     P0 из трёх пунктов схлопнулся в один — сборка ESP на железе.
            Закрыты HYP-007 и HYP-011, обе ОПРОВЕРГНУТЫ.
            WARN-016 понижен до P3 с обоснованием, почему он латентный.
            errors.txt: 3 записи, из них ERR-001 в статусе ЖДЁТ ПРОВЕРКИ.
            warnings.txt: 20 записей, не трогались.
            i-think-it-can-be-error.txt: 15 гипотез (было 17).

Скан #3 — 2026-08-21, починка всего P1 плюс сверка документации с кодом.
  Охват:    firmware/ (Core + OrcaKernel) читался по всем открытым записям;
            firmware/esp32/main/orca_wifi.c и orca_cfg.c — чтением;
            tools/module_sdk (шаблон и пример esp_link) — со сборкой;
            docs/ — architecture.md, TASKS.md на расхождения с кодом.
            Не читались: firmware/build/_deps/**, бинарные артефакты.
            Каталогов app/ и esp32/main/web/ в дереве уже нет — часть
            записей закрывалась именно этим фактом, а не разбором кода.
  Проверки: firmware — cmake --build build -j после каждой правки, финальная
            сборка чистая: FLASH 637 328 Б (30.39%), RAM_D1 232 672 Б.
            arm-none-eabi-nm — _end 0x2402ccd8, _heap_limit 0x24080000;
            все 22 хелпера __aeabi_* в образе.
            SDK — make в examples/esp_link даёт esp_link.orca 1700 Б,
            make check-symbols печатает пустой список; build/ убран.
            esp32 — idf.py build ПО-ПРЕЖНЕМУ не запускался, ESP-IDF нет
            в PATH (command -v idf.py и $IDF_PATH пусты). Правки в
            orca_wifi.c проверены только чтением.
            Железо — ничего: ни run модуля, ни трансляции, ни SD под
            нагрузкой. Каждая закрытая запись честно говорит, чего в её
            «Проверке» не хватает.
  Итог:     errors.txt — 0 открытых (было 3). warnings.txt — 4 открытых:
            WARN-010, WARN-011, WARN-018, WARN-019 (было 12).
            i-think-it-can-be-error.txt — 5 гипотез: HYP-004, HYP-006,
            HYP-008, HYP-009, HYP-017 (было 15).
            P0 — один пункт, тот же: собрать ESP32. P1 — пуст.
            Заодно исправлены три расхождения в самих файлах-трекерах:
            -mlong-calls в architecture.md §3, адрес get_fattime() в
            WARN-018 (bsp_diskio.c, а не bsp_sdmmc.c), путь в HYP-004.
  Что сделать в следующий раз в первую очередь:
    1. Поставить ESP-IDF и прогнать idf.py build — это последний P0.
       Заодно проверится всё, что в esp32/ пока подтверждено только
       чтением: HYP-002, WARN-001, WARN-014.
    2. Инициализировать git до того, как правок станет больше (P3).
       Без .gitignore первый коммит утащит firmware/build/_deps.
    3. Начать Milestone 2 — дисплей. Пока LTDC нет, картинку видно только
       через fbstream, и рисует туда одна тестовая задача.

